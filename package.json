{
  "name": "gsg-mermaid-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first Mermaid diagram source editor and viewer. Static runtime; no npm install required.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "build:portable": "python3 scripts/build_release.py"
  },
  "engines": {"node": ">=18"}
}
